 <button class="drop-btn" id="search-btn"><img src="./src/assets/menu.svg" alt="menu"
                title="Search With Any S-Engin"></button>

        <div id="search-popup" class="pop-search">

.drop-btn {
    height: inherit;
    width: 50px;
    background: transparent;
    border: none;
    margin: auto;
    margin-right: 5px;
.drop-btn img {
    height: inherit;
    content: url(../assets/menu.svg);



// Elements for the 'History Box' popup
const openSearchPopupBtn = document.getElementById("search-btn");
const searchPopup = document.getElementById("search-popup");
const searchOverlay = document.getElementById("search-overlay");

// Event listener to open the 'SearchHistory Box' popup.
openSearchPopupBtn.addEventListener("click", () => {
  searchPopup.style.display = "flex";
  searchOverlay.style.display = "block";
});

// Event listener to close the 'History Box' popup when clicking its overlay.
searchOverlay.addEventListener("click", () => {
  searchPopup.style.display = "none";
  searchOverlay.style.display = "none";
});